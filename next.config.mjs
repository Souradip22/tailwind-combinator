/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "souradip.ch",
      "images.unsplash.com",
      "portfoliominute.in",
      "source.unsplash.com",
    ],
  },
};

export default nextConfig;
